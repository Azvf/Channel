# GameplayTag 文档中心

## 文档结构说明

根据**"代码即文档"**和**"工具强制"**的理念，文档已重构为**稳定层（Principles）**和**易变层（Reference）**：

### 稳定层（Principles）- 极少变动

这些文档阐述**"为什么我们要这么做"**，是项目的灵魂：

1. **[框架开发规范文档](./GameplayTag%20框架开发规范文档.md)**
   - 分层架构原则
   - 依赖方向规则
   - 设计模式与最佳实践
   - **架构守护**: 依赖规则由 `dependency-cruiser` 强制执行

2. **[交互与体验开发手册](./GameplayTag%20交互与体验开发手册.md)**
   - 乐观更新哲学
   - 物理感核心原则
   - 渲染性能规范（RAIL 模型）
   - **架构守护**: 乐观更新提醒由 ESLint 规则强制执行

3. **[无障碍与工程化规范](./GameplayTag%20无障碍与工程化规范.md)**
   - 无障碍访问标准
   - 测试策略
   - 代码质量规范

### 易变层（Reference）- 自动生成/指向代码

这些文档是**"怎么做"**的参考手册，每次 Commit 都可能变化：

1. **[设计系统规范](./GameplayTag%20Design%20System%20Specification.md)**
   - **单一真理源**: 所有设计值定义在 `src/design-tokens/tokens.ts`
   - 文档仅说明语义，具体数值由 Token 系统管理
   - 参考: Storybook "Design Tokens" 章节

2. **API 契约**
   - RPC 接口定义: `src/shared/rpc-protocol/protocol.ts`
   - 建议使用 TypeDoc 自动生成文档

3. **组件库**
   - 组件 Props 和用法: Storybook
   - 运行: `npm run storybook`

## 架构守护工具

### 🚀 自动构建时检查

**重要**: 所有架构检查已集成到构建流程中！

运行 `npm run build` 时会自动执行：
- ✅ TypeScript 类型检查
- ✅ ESLint 检查（包含自定义规则）
- ✅ 依赖架构检查（dependency-cruiser）
- ✅ Design Tokens 生成

如果任何检查失败，构建将停止。这确保了生产代码始终符合架构规范。

> 详细说明请查看 [构建时架构检查说明](./构建时架构检查说明.md)

### 手动运行检查

#### 依赖规则检查

```bash
npm run check:arch
```

检查分层架构是否被违反。如果有人在 `src/core` 中 `import React`，构建将直接失败。

#### ESLint 检查

```bash
npm run lint        # 检查
npm run lint:fix    # 自动修复
```

> **注意**: 自定义规则（`no-raw-z-index`、`require-optimistic-update`）需要正确配置 ESLint 插件格式，目前暂时禁用。架构检查主要通过 `dependency-cruiser` 和 TypeScript 类型检查来保证。

#### Design Tokens 生成

```bash
npm run generate:tokens
```

从 `src/design-tokens/tokens.ts` 生成 CSS 变量。

#### 运行所有预构建检查

```bash
npm run prebuild:checks
```

## 文档更新原则

1. **如果是规则，就用 Lint 报错**
2. **如果是常量，就用 Token 生成**
3. **如果是流程，就用 Mermaid 绘图**
4. **如果是性能，就用指标量化**

这样，文档将不再是一份需要死记硬背的"法条"，而是一份阐述"为什么我们要这么做"的**战略指南**。

