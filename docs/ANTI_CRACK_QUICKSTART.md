# 快速开始：如何保护您的浏览器插件

本文档提供快速上手指南，帮助您在几分钟内为插件添加基础的防护措施。

## 🚀 立即生效的保护措施

### 1. 安装混淆工具

```bash
npm install
```

这将安装 `javascript-obfuscator` 工具。

### 2. 使用生产构建命令

```bash
# 普通构建（开发使用）
npm run build

# 生产构建（含混淆保护）
npm run build:prod
```

### 3. 手动混淆（可选）

如果只想混淆已构建的文件：

```bash
npm run obfuscate
```

## 📋 保护效果

使用 `build:prod` 后，您的代码将获得：

✅ **代码混淆** - 变量名、函数名被随机化  
✅ **无 Source Maps** - 无法还原原始代码结构  
✅ **控制台清理** - 移除了调试信息  
✅ **代码扁平化** - 执行流程被打乱  
✅ **字符串编码** - 字符串被 base64 编码  
✅ **自我防护** - 代码被篡改时会自动禁用  

## 🔍 对比效果

### 混淆前：
```javascript
function createTag(name, description, color) {
  const tag = {
    id: generateId(),
    name: name.trim(),
    description: description,
    color: color || getRandomColor()
  };
  tags[tag.id] = tag;
  return tag;
}
```

### 混淆后：
```javascript
var _0x4f2a=['cmV0dXJu','dHJpbQ==','b3BhY2l0eQ=='];(function(_0x123abc,_0x456def){var _0x789ghi=_0x1234ab;while(!![]){try{var _0xabcdef=-parseInt(_0x789ghi(0x0))*parseInt(_0x789ghi(0x1));}catch(_0xerr){break;}}}(_0x4f2a,0x123));
function _0xa1b2c3(_0xdef456,_0xghi789,_0xjkl012){var _0x345abc=generateId();var _0x678def={id:_0x345abc,name:_0xdef456[_0x4f2a[1]]()};tags[_0x345abc]=_0x678def;return _0x678def;}
```

## ⚠️ 重要提示

1. **开发时不要使用混淆** - 只在生产发布时使用
2. **测试混淆后的代码** - 混淆可能导致性能问题
3. **备份构建产物** - 混淆后的代码难以调试

## 🔧 自定义配置

如果需要调整混淆强度，编辑 `obfuscator.config.js`：

```javascript
module.exports = {
  // 更高强度（可能影响性能）
  controlFlowFlatteningThreshold: 1.0,
  stringArrayThreshold: 1.0,
  
  // 禁用某些特性（提升性能）
  selfDefending: false,
  debugProtection: false
};
```

## 📚 了解更多

查看完整防护指南：[ANTI_CRACK_GUIDE.md](./ANTI_CRACK_GUIDE.md)

包含：
- License 验证
- 服务器端保护
- 运行时检测
- 法律保护
- 高级防护策略

## 🆘 故障排查

### 混淆后代码无法运行

**问题**：可能是因为混淆配置太激进

**解决**：降低混淆强度
```javascript
// obfuscator.config.js
controlFlowFlatteningThreshold: 0.25,  // 降低扁平化
selfDefending: false                     // 禁用自我防护
```

### 混淆后文件太大

**问题**：混淆可能导致文件体积增大

**解决**：减少字符串数组操作
```javascript
stringArrayThreshold: 0.3,  // 只转换30%的字符串
stringArrayEncoding: []      // 禁用编码
```

### 混淆时间过长

**问题**：项目较大时混淆可能很慢

**解决**：只混淆关键文件
```javascript
// obfuscate.js - 只混淆后台服务
const filesToObfuscate = [
  'dist/background.js'  // 只混淆最重要的文件
];
```

## 🎯 推荐策略

对于浏览器插件，建议采用**分层防护**：

### 第一层：基础混淆（已实现）
- ✅ 代码混淆
- ✅ Source Maps 禁用
- ✅ Console 清理

### 第二层：License 验证
```bash
# 添加 license 验证服务
npm install crypto-js
```

### 第三层：服务器端逻辑
- 将核心功能移至服务器 API
- 定期向服务器验证状态

### 第四层：持续监控
- 收集异常行为
- 定期更新版本
- 发布安全补丁

## 📞 需要帮助？

- 查看详细文档：`docs/ANTI_CRACK_GUIDE.md`
- 检查混淆配置：`obfuscator.config.js`
- 测试构建产物：`dist/` 目录

