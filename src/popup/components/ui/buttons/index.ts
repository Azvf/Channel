// Re-export shadcn Button and IconButton for backward compatibility
export { Button, IconButton, buttonVariants } from '../button';
export type { ButtonProps, IconButtonProps } from '../button';

// Legacy exports (deprecated, use Button and IconButton from '../button' instead)
export { Button as ShadcnButton } from '../button';

